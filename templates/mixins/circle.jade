mixin circle(members, tag, memberStyle)
	.row
		.chip=tag
		span.new.badge=members.length
	- var i=0;
	- while(i<members.length)
		.row
			- var j=0;
			- while(j<4 && i<members.length)
				.col.l3
					.ih-item.circle(class=memberStyle)
						a.modal-trigger.neo-member(href='#memberModal',data-member=members[i].id)
							.spinner
							.img
								img(src=members[i].photo._.image.src())
							.info
								.info-back
									h3=members[i].name.first+members[i].name.last
									p=members[i].desciption
									
				- j++
				- i++